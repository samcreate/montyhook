'use strict';
module.exports = function(sequelize, DataTypes) {
  var IntentVarietals = sequelize.define('IntentVarietals', {
    weight: {
      type: DataTypes.INTEGER,
      defaultValue: 0
    }
  }, {
    classMethods: {
      associate: function(models) {
        // associations can be defined here
      }
    }
  });
  return IntentVarietals;
};
